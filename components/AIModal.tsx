import React from 'react';

// Placeholder component. AI features are handled by specific modals like SummarizerModal, etc.
const AIModal: React.FC = () => {
    return null;
};

export default AIModal;
